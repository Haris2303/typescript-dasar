describe("Data Type", () => {
    it("should must declare", () => {
        let name: string = "Otong Surotong";
        let balance: number = 100;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
